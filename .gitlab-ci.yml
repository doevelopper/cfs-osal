#
# To check this script syntax: https://gitlab.com/doevelopper/cfs-osal/-/ci/lint
#
# sudo usermod -a -G sudo gitlab-runner
# sudo visudo or sudo vi /etc/sudoers
# %gitlab-runner ALL=(ALL) NOPASSWD: ALL

stages:
    - lifecycle
    - Sw Quality Assessment
    - code review
    - documentations
    - deploy

include:
    - local: .gitlab/ci/globals.yml
    # - local: .gitlab/ci/debug-build.yaml
    # - local: .gitlab/ci/release-build.yaml
    # - local: .gitlab/ci/profiling-build.yaml
    # - local: .gitlab/ci/coverage-build.yaml
    # - local: .gitlab/ci/binary-code-performance-testing.yml
    # - local: .gitlab/ci/code-duplicate-detection-check.yml
    # - local: .gitlab/ci/continous-code-quality-check.yml
    # - local: .gitlab/ci/dynamic-application-security-testing.yml
    # - local: .gitlab/ci/dynamic-code-analysis-check.yml
    # - local: .gitlab/ci/run-time-application-security-protection.yml
    # - local: .gitlab/ci/static-application-security-testing.yml
    # - local: .gitlab/ci/build-lifecycle.yml
    # - local: .gitlab/ci/clean.yml
    # - local: .gitlab/ci/applications-qa.yml
    # - local: .gitlab/ci/applications-review.yml
    # - local: .gitlab/ci/applycations-deployment.yml
    # - local: .gitlab/ci/mirrors.yml
    # - local: .gitlab/ci/ssh.yml

.release:appplication:build:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.coverage:appplication:build:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Coverage
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.profiling:appplication:build:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Profiling
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.relwithdebinfo:appplication:build:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: RelWithDebInfo
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.minsizerel:appplication:build:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: MinSizeRel
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Code duplicate detection:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Continuous code quality:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Dynamic code analysis:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Dynamic application security testing:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Static application security testing:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Run time application security protection:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Binary code performance:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""

.Publish code quality:
    extends:
        .build-applications
    variables:
        BUILD_TYPE: Release
        BUILD_OPTIONS: ""
        BUILD_IMAGE: ""


